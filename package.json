{
  "name": "resume-analyzer-backend",
  "version": "1.0.0",
  "description": "Secure backend proxy for Resume Analyzer",
  "main": "api/analyze.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "22.x"
  }
}
